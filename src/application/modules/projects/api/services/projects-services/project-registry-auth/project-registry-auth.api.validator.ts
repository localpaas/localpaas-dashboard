import { RegistryAuthApiValidator } from "~/settings/api/services/registry-auth-services";

/**
 * Same response shapes as global registry-auth; only URL paths differ.
 */
export class ProjectRegistryAuthApiValidator extends RegistryAuthApiValidator {}
