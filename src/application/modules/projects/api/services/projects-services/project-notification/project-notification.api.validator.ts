import { NotificationsApiValidator } from "~/settings/api/services/notifications-services";

/**
 * Same response shapes as global notifications; only URL paths differ.
 */
export class ProjectNotificationApiValidator extends NotificationsApiValidator {}
